name: Flutter CI
on:
  push:
    branches: [arena/01a0a0bf-sahlha-ai-app, main]
    paths: ["mobile/**", ".github/workflows/flutter-ci.yml"]
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Smoke test
        run: echo "CI is alive on $(date -u)"
